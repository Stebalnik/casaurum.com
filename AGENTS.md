# Project Rules

- Before starting any new work, inspect the current git status and review existing changes.
- If there are uncommitted changes, commit them first as a separate checkpoint before beginning the new task, unless the user explicitly asks not to commit or committing is impossible.
- Do not overwrite or revert existing user changes without explicit instruction.
